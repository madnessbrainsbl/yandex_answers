Ограничение времени	1 секунда
Ограничение памяти	64 Мб
Ввод	стандартный ввод или input.txt
Вывод	стандартный вывод или output.txt
Системному администратору Василию выпала задача о подсчете объёма всех полноценных массивов дисков серверного парка его компании.

Василий обратился к бухгалтерам и ему сделали выгрузку из учётной системы оборудования в формате CSV. Для каждой единицы оборудования в выгрузке используется один файл, в котором перечислены его составные части. То есть для каждого сервера в выгрузке должен быть один CSV-файл с перечислением оборудования, из которого он состоит. Разделителем в файлах выгрузки является запятая — ,. Интересующие Василия строчки выглядят следующим образом:

<инвентарный номер устройства>,<тип устройства>,<основное свойство устройства>
В случае дисков это будут строчки вида (для диска с инвентарным номером 1 и размером 128 гигабайт):

1,Disk,137438953472
В компании Василия принято собирать диски в серверах в RAID10, при невозможности собрать RAID10, собирается RAID1. Когда Василий взглянул на выгрузку, которую ему предоставили бухгалтеры, он обнаружил, что не все файлы в ней являются серверами! Там оказались выгрузки целых складов и дата-центров, а также, по какой-то причине, отдельные полки на складах. Василий быстро вывел критерии того, что файл содержит полноценный сервер (а не дата-центр или полку на складе) — размер CSV-файла не превышает 1 КБ, и при этом больше 100 байт. Помогите Василию обработать выгрузку и получить объём всех полноценных RAID10 и RAID1 массивов дисков в его компании.

Максимальный балл за эту задачу — 8.

Формат ввода
На вход поступает строка, содержащая префикс файлов выгрузки, которые необходимо обработать. Например, если выгрузка состоит из файлов server1.csv и server2.csv, то на вход будет подана строка server. Необходимо обработать файлы в текущей рабочей директории, удовлетворяющие условиям:

Совпадает переданный префикс
Файл содержит полноценную выгрузку сервера
Формат вывода
На выходе ожидается одно число - сумма размеров всех полноценных RAID1 и RAID10 массивов всех обработанных серверов из выгрузки.
